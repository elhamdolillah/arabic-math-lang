# سجل أرشيف UORI الكامل

هذا الملف يصف نسخة الاستعادة الكاملة لمشروع UORI في وقت إنشاء الأرشيف.

## المحتويات

يتضمن الأرشيف جذر مستودع `arabic-math-lang` كاملاً، بما في ذلك ملفات المصدر، التوثيق العربي والإنجليزي، الأدلة، الاختبارات، ملفات `vnext`، النماذج الرسمية، ملفات Assembly، ملفات WASM/WAT، الأمثلة، المعاجم، المخططات، وسجل Git المحلي `.git` لاستعادة التاريخ والفروع المحلية.

كما يتضمن مجلد `supplementary/` نسخة من مشروع الشرائح الموجود في `secure-gateway-file-test-slides` ونسخة من الملف المرفق `pasted_content.txt` بوصفه مادة مرجعية مستقلة.

## قواعد الاستعادة

1. فك الضغط إلى مسار جديد.
2. ادخل إلى مجلد `arabic-math-lang`.
3. افحص سلسلة `UORI_DOCS_CHAIN.sha256` بالأمر `sha256sum -c UORI_DOCS_CHAIN.sha256`.
4. شغّل الاختبارات من جذر المستودع مع ضبط `PYTHONPATH=.`.
5. لا تُشغّل أي مصدر خارجي قبل المرور ببوابة القبول والتفويض.

## حدود الأرشيف

لا يتضمن الأرشيف محتويات `/home/ubuntu/upload` كاملة، ولا الأسرار أو رموز الدخول أو ملفات المتصفح المؤقتة. أُدرج الملف المرفق المطلوب فقط، لأن تضمين مجلد الرفع كله قد يكشف ملفات لا تخص المشروع.

وجود ملفات Coq أو Rust أو WASM في الأرشيف لا يساوي إثبات تشغيلها على عتاد حقيقي؛ وتبقى حالات `ABSTAIN` و`NOT_IMPLEMENTED` محفوظة كما هي في الأدلة.

## هوية المصدر

المستودع الأصلي: `https://github.com/elhamdolillah/arabic-math-lang.git`

يجب مقارنة بصمة الأرشيف وبصمات الملفات مع سجل Git وسلسلة الأدلة بعد الاستعادة.

## تاريخ الإنشاء

يُسجل تاريخ الإنشاء ورقم الالتزام النهائي في ملف `ARCHIVE_RELEASE_METADATA.txt` المرافق للأرشيف.
