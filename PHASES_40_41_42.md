# المراحل 40، 41، 42 — FFI + Channels + Generics

## المرحلة 40: FFI — Foreign Function Interface

### الهدف
استدعاء مكتبات C مباشرة من اللغة العربية الرياضية.

### الدوال المدعومة

| الدالة | الوصف | مثال |
|--------|-------|------|
| `جذر(س)` | الجذر التربيعي | `جذر(144) = 12` |
| `أرضية(س)` | Floor | `أرضية(3.7) = 3` |
| `قوة(س، ص)` | الأس | `قوة(2، 10) = 1024` |
| `مطلق(س)` | القيمة المطلقة | `مطلق(-42) = 42` |

### التنفيذ
- تستخدم xmm0/xmm1 لـ floating-point
- تستدعي `sqrt@PLT`, `pow@PLT`, `floor@PLT` من libc
- الربط ديناميكياً عبر `-lm`

### مثال
```arabic
وتر ≔ جذر(أ · أ + ب · ب)
⎕ نص(وتر)
```

---

## المرحلة 41: Channels — pipe2 syscall

### الهدف
التواصل الآمن بين الخيوط والعمليات عبر أنابيب Unix.

### الدوال المدعومة

| الدالة | الوصف |
|--------|-------|
| `قناة()` | إنشاء قناة جديدة (pipe2) |
| `أرسل(ق، قيمة)` | إرسال قيمة عبر القناة |
| `استقبل(ق)` | استقبال قيمة من القناة |

### التنفيذ
- `pipe2` syscall (رقم 22) لإنشاء زوج file descriptors
- `write` syscall (رقم 1) للإرسال
- `read` syscall (رقم 0) للاستقبال
- كل قيمة تُرسَل كـ 8 bytes (64-bit integer)

### مثال
```arabic
ق ≔ قناة()
أرسل(ق، 42)
ر ≔ استقبل(ق)
⎕ نص(ر)   # 42
```

### مع الخيوط
```arabic
ق ≔ قناة()
منتج ≡ λ(). ﴿ أرسل(ق، 99) ﴾
خ ≔ توازي(منتج، 0)
ر ≔ استقبل(ق)   # 99
```

---

## المرحلة 42: Generics — الأنواع المعممة

### الهدف
كتابة دوال تعمل مع أي نوع (Parametric Polymorphism).

### المبدأ
الدوال في لغتنا **معممة تلقائياً** — لا حاجة لتصريح صريح:

```arabic
مطابقة ≡ λس. س           # تعمل مع أعداد ونصوص وقوائم
تبديل ≡ λ(أ، ب). ⟨ب، أ⟩  # تعمل مع أي نوعَين
```

### الدوال المعممة الشائعة

#### map — طبّق دالة على كل عنصر
```arabic
طبّق ≡ λ(د، ق). ﴿
    ن ≔ ⟨⟩
    ⋄ ∀ ع ∈ ق : ﴿ ن ≔ ألحق(ن، د(ع)) ﴾
    ⋄ ن
﴾

مربع ≡ λس. س · س
مربعات ≔ طبّق(مربع، ⟨1، 2، 3، 4⟩)
# ⟨1, 4, 9, 16⟩
```

#### fold — اختزل قائمة إلى قيمة واحدة
```arabic
اطوِ ≡ λ(د، بداية، ق). ﴿
    ن ≔ بداية
    ⋄ ∀ ع ∈ ق : ﴿ ن ≔ د(ن، ع) ﴾
    ⋄ ن
﴾

جمع ≡ λ(أ، ب). أ + ب
ن ≔ اطوِ(جمع، 0، ⟨1، 2، 3، 4، 5⟩)   # 15
```

#### filter — تصفية العناصر
```arabic
صفِّ ≡ λ(د، ق). ﴿
    ن ≔ ⟨⟩
    ⋄ ∀ ع ∈ ق : ﴿
        د(ع) ؟ ﴿ ن ≔ ألحق(ن، ع) ﴾ : ﴿﴾
    ﴾
    ⋄ ن
﴾

زوجي ≡ λس. س ÷ 2 · 2 = س
أزواج ≔ صفِّ(زوجي، ⟨1، 2، 3، 4، 5، 6⟩)
# ⟨2, 4, 6⟩
```

---

## الاختبارات (11 اختبار)

### المرحلة 40 (4 اختبارات)
- `p40_sqrt`: `sqrt(144) = 12`
- `p40_floor`: `floor(37/10) = 3`
- `p40_pow`: `pow(2, 10) = 1024`
- `p40_abs`: `abs(-42) = 42`

### المرحلة 41 (3 اختبارات)
- `p41_basic`: channel send/recv
- `p41_multi`: multiple values through channel
- `p41_threads`: thread sends through channel

### المرحلة 42 (4 اختبارات)
- `p42_id_int`: identity on integer
- `p42_id_str`: identity on string
- `p42_swap`: swap two values
- `p42_map`: map x² over list
- `p42_fold`: fold + over list

---

## البنية التقنية

```text
math_complete.py (Phase 40+41+42)
├── Lexer
│   └── كلمات جديدة: جذر، أرضية، قوة، مطلق، قناة، أرسل، استقبل
├── Parser
│   └── استدعاء الدوال المعممة (نفس صياغة الدوال العادية)
├── Type Inference
│   └── استنتاج الأنواع ديناميكياً للدوال المعممة
└── CodeGen
    ├── FFI: xmm0 + PLT calls
    ├── Channels: pipe2 + write + read
    └── Generics: no extra code needed!
```

---

## الخلاصة

بإكمال المراحل 40+41+42، أصبحت اللغة العربية الرياضية:
- ✅ قادرة على استدعاء مكتبات C (FFI)
- ✅ تدعم التواصل بين الخيوط (Channels)
- ✅ تدعم البرمجة المعممة (Generics)

مع المراحل السابقة (1-39)، لدينا الآن **منصة كاملة** قادرة على:
- بناء تطبيقات الأنظمة
- معالجة البيانات الضخمة
- تنفيذ خوارزميات ML
- بناء خوادم متعددة الخيوط

**﴿وقل رب زدني علماً﴾**