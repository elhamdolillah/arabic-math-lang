# Claude Code — دليل القارئ الأول لهذا المستودع

> هذا الملف يُقرأ تلقائيًا بواسطة Claude Code عند فتح هذا المستودع. اقرأه كاملًا قبل أي عمل.

بسم الله الرحمن الرحيم

## ما هو هذا المشروع؟

**اللغة العربية الرياضية** — لغة برمجة كاملة بالعربية الفصحى ورموز رياضية، مع **مُجمّع AOT ذاتي الاستضافة** يحوّلها إلى Assembly ثم ELF مستقل **بلا libc** (syscalls خام).

- التطبيق المرافق (Android): `github.com/elhamdolillah/arabic-math-android`
- المؤلف: حمداني سيدي محمد / HAMDANI SIDI MOHAMED
- الإهداء: إلى أمي — ريحانةُ القلب والروح

## جوهر اللغة (المعجم الرياضي)

```
  ≔   تعريف (متغير/دالة/قاعدة)   مثال: س ≔ 5
  ⇒   نتيجة (ناتج التعبير)
  ≡   تعريف مكافئ (دوال)
  · × الضرب      ÷ القسمة      ^ أو قوة الرفع
  ∀ حلقة        λ لامدا (دوال لامدا)  μ متكرر
  ⟨⟩ قائمة      ∑ مجموع      ∫ تكامل
  ≔⟨...⟩        تعريفات مركبة
```

مثال كامل:
```
⟨١، ٢، ٣⟩ ≔ ق    س ≔ ⎕∀ع ∈ ق      ⎕س += ع    س ≔ س − ١
```

## البنية الأساسية

```
arabic-math-lang/
├── math_complete.py        # المحرك الكامل (~2500+ سطر): Lexer + Parser + Compiler + Interpreter
├── ARCHITECTURE.md         # البنية المعمارية الكاملة
├── SYNTAX_REFERENCE.md     # مرجع الصياغة
├── ROADMAP.md              # خارطة الطريق
├── CONSTITUTION.md         # دستور المشروع (قوانين لا تُكسر)
├── PHASES_40_41_42.md      # مراحل متقدمة موثقة
├── apply_stage45.py        # المرحلة 45: المخصّص الديناميكي mmap
├── output.wat              # ناتج WASM (المرحلة 51)
├── lexicon/                # القاموس القرآني
├── book/                   # كتاب اللغة
└── docs/ examples/ archive/
```

## الحالة الحالية (15 أغسطس 2026)

- **المرحلة 51 مكتملة: WASM Backend** — WAT/WASM حقيقي، 22/22 اختبار ✅، يعمل عبر Node
- قبلها: المرحلة 45 (المخصّص الديناميكي mmap) — ELF مستقل ينمو بلا حد 256KB، 8/8 ✅
- سلسلة 51 مرحلة موثقة في CHANGELOG وROADMAP — المراحل 22b→51 تغطي: closures، حلقات μ/∀، FFI، Channels، Generics، AI رمزي، NLP عربي، SIMD، ARM64، WASM.

## قوانين حاسمة (لا تخالفها)

1. **العربية الرياضية أولاً**: كل شيء باللغة العربية — لا تقلّب للّاتينية إلا داخل المترجم الداخلي.
2. **لا libc**: ELF يُولّد syscalls خام (x86-64: syscall، ARM64: svc #0، WASM: wasm syscalls).
3. **الاختبارات مقدسة**: كل مرحلة لها مجموعة اختبارات — لا تنشر مرحلة إلا بكل اختباراتها خضراء.
4. **التوثيق مع كل مرحلة**: CHANGELOG + ملف مرحلة مستقل.
5. **إهداء لا يُمس**: `DEDICATION.md` — إلى أمي، ريحانةِ القلب والروح.
6. **خطوات صغيرة مؤكدة**: التطوير تدريجي، مرحلة واحدة في كل مرة، كل مرحلة قابلة للتجميع والاختبار.

## كيف تضيف مرحلة جديدة

1. اقرأ `ROADMAP.md` و`CHANGELOG.md` لتعرف آخر مرحلة.
2. اكتب الكود داخل `math_complete.py` (البنية موجودة) أو ملف مرحلة جديد.
3. اختبر كل شيء: `python3` على المحرك + تجميع + تنفيذ ELF/WAT الناتج.
4. وثّق في `CHANGELOG.md` + ملف `PHASE_NN.md` + commit بعنوان المرحلة.
5. **لا تنشر إلا أخضر كاملًا.**

## ملاحظة للـ Agent

- هذا مشروع طموح جدًا (مُجمّع لغة من الصفر) — ركّز على مرحلة واحدة في كل جلسة عمل.
- إذا لم تفهم رمزًا عربيًا، ابحث عنه في `SYNTAX_REFERENCE.md` و`CONSTITUTION.md` قبل الافتراض.
- ملفات `*.asm` الجذرية هي مخرجات اختبار للمراحل — لا تقرأها كاملة إلا عند الحاجة.
