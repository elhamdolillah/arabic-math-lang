#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
المرحلة 25: المؤشرات (دليل)
الدستور: ﴿آيات بينات في صدور الذين أوتوا العلم﴾
"""
import sys, subprocess
sys.path.insert(0, '.')
from math_complete import *

# ═══════════════════════════════════════════════════════════
# اختبار 1: قراءة عبر المؤشر (رأس القائمة)
# ═══════════════════════════════════════════════════════════
print("=" * 70)
print("المرحلة 25: المؤشرات (دليل)")
print("الدستور: ﴿آيات بينات في صدور الذين أوتوا العلم﴾")
print("=" * 70)

failed = 0

# ─────────────────────────────────────────────────────────
# اختبار 1: قراءة عبر المؤشر
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 1: قراءة عبر المؤشر (﴿فأينما تولوا فثم وجه الله﴾)")
برنامج_قراءة = """مخزن ≔ ⟨42, 10, 25⟩
قيمة١ ≔ رأس(مخزن)
قيمة٢ ≔ رأس(ذيل(مخزن))
قيمة٣ ≔ رأس(ذيل(ذيل(مخزن)))
⎕ "الموقع 0: " ⊕ نص(قيمة١)
⎕ "الموقع 1: " ⊕ نص(قيمة٢)
⎕ "الموقع 2: " ⊕ نص(قيمة٣)
"""

ر = حلل_رموز(برنامج_قراءة)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('ptr1.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'ptr1.asm', '-o', 'ptr1.o'], check=True)
subprocess.run(['ld', 'ptr1.o', '-o', 'ptr1'], check=True)
result = subprocess.run(['./ptr1'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "الموقع 0: 42" in out and "الموقع 1: 10" in out and "الموقع 2: 25" in out:
    print("  ✅ القراءة عبر المؤشر تعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 2: كتابة عبر المؤشر (تحديث قيمة)
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 2: كتابة عبر المؤشر (﴿يبدل الله سيئاتهم حسنات﴾)")
برنامج_كتابة = """مخزن ≔ ⟨42, 10, 25⟩
مخزن ≔ ألحق(ذيل(مخزن)، 100)
قيمة ≔ رأس(ذيل(ذيل(مخزن)))
⎕ "القيمة بعد الكتابة: " ⊕ نص(قيمة)
"""

ر = حلل_رموز(برنامج_كتابة)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('ptr2.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'ptr2.asm', '-o', 'ptr2.o'], check=True)
subprocess.run(['ld', 'ptr2.o', '-o', 'ptr2'], check=True)
result = subprocess.run(['./ptr2'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج: {out}")

if "القيمة بعد الكتابة: 100" in out:
    print("  ✅ الكتابة عبر المؤشر تعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 3: مؤشرات النصوص (قائمة نصوص)
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 3: مؤشرات النصوص (﴿بلسان عربي مبين﴾)")
برنامج_نصوص = """أسماء ≔ ⟨"أحمد", "فاطمة", "علي"⟩
اسم١ ≔ رأس(أسماء)
اسم٢ ≔ رأس(ذيل(أسماء))
⎕ "الاسم الأول: " ⊕ اسم١
⎕ "الاسم الثاني: " ⊕ اسم٢
"""

ر = حلل_رموز(برنامج_نصوص)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('ptr3.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'ptr3.asm', '-o', 'ptr3.o'], check=True)
subprocess.run(['ld', 'ptr3.o', '-o', 'ptr3'], check=True)
result = subprocess.run(['./ptr3'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "الاسم الأول: أحمد" in out and "الاسم الثاني: فاطمة" in out:
    print("  ✅ مؤشرات النصوص تعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 4: مؤشرات + ملفات (حفظ واسترجاع)
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 4: مؤشرات + ملفات (﴿في لوح محفوظ﴾)")
برنامج_ملفات = """مخزن ≔ ⟨42, 10, 25⟩
قيمة ≔ رأس(مخزن)
ملف ≔ فتح("ptr_test.txt")
اكتب_ملف(ملف، نص(قيمة))
اختم(ملف)
ملف ≔ فتح("ptr_test.txt")
محتوى ≔ اقرأ_ملف(ملف، 4096)
اختم(ملف)
⎕ "القيمة المحفوظة: " ⊕ محتوى
"""

ر = حلل_رموز(برنامج_ملفات)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('ptr4.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'ptr4.asm', '-o', 'ptr4.o'], check=True)
subprocess.run(['ld', 'ptr4.o', '-o', 'ptr4'], check=True)
result = subprocess.run(['./ptr4'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج: {out}")

if "القيمة المحفوظة: 42" in out:
    print("  ✅ مؤشرات + ملفات تعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 5: حلقة على المؤشرات
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 5: حلقة على المؤشرات (﴿كل في فلك يسبحون﴾)")
برنامج_حلقة = """أعداد ≔ ⟨10, 20, 30, 40, 50⟩
مجموع ≔ 0
∀ س ∈ أعداد : ﴿ مجموع ≔ مجموع + س ﴾
⎕ "المجموع: " ⊕ نص(مجموع)
⎕ "العدد: " ⊕ نص(أحص(أعداد))
"""

ر = حلل_رموز(برنامج_حلقة)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('ptr5.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'ptr5.asm', '-o', 'ptr5.o'], check=True)
subprocess.run(['ld', 'ptr5.o', '-o', 'ptr5'], check=True)
result = subprocess.run(['./ptr5'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "المجموع: 150" in out and "العدد: 5" in out:
    print("  ✅ حلقة على المؤشرات تعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ═══════════════════════════════════════════════════════════
# الملخص
# ═══════════════════════════════════════════════════════════
print("\n" + "=" * 70)
print("📖 الدستور القرآني المطبق:")
print("   ﴿آيات بينات في صدور الذين أوتوا العلم﴾ — المؤشر آية")
print("   ﴿لكل نبأ مستقر﴾ — كل قيمة لها مستقر")
print("   ﴿فأينما تولوا فثم وجه الله﴾ — أينما يشير المؤشر")
print("   ﴿يبدل الله سيئاتهم حسنات﴾ — التعديل عبر المؤشر")
print("   ﴿بلسان عربي مبين﴾ — مؤشرات النصوص")
print("   ﴿في لوح محفوظ﴾ — الحفظ في ملفات")
print("   ﴿كل في فلك يسبحون﴾ — الحلقات على المؤشرات")

print("\n" + "=" * 70)
if failed == 0:
    print(f"🎉 المرحلة 25 نجحت!")
    print(f"   لغتنا العربية تدعم المؤشرات (دليل):")
    print(f"   - قراءة عبر المؤشر (رأس، ذيل)")
    print(f"   - كتابة عبر المؤشر (ألحق)")
    print(f"   - مؤشرات النصوص")
    print(f"   - مؤشرات + ملفات")
    print(f"   - حلقات على المؤشرات (∀)")
    print(f"\n   الخطوة التالية: المرحلة 26 — الشبكة (عروة)")
else:
    print(f"❌ فشل {failed} اختبار")
    sys.exit(1)