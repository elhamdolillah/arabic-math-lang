#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
المرحلة 30: AI الرمزي (علم آدم)
الدستور: ﴿وعلم آدم الأسماء كلها﴾
"""
import sys, subprocess
sys.path.insert(0, '.')
from math_complete import *

# ═══════════════════════════════════════════════════════════
print("=" * 70)
print("المرحلة 30: AI الرمزي (علم آدم)")
print("الدستور: ﴿وعلم آدم الأسماء كلها﴾")
print("=" * 70)

failed = 0

# ─────────────────────────────────────────────────────────
# اختبار 1: قاعدة المعرفة (لوح محفوظ)
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 1: قاعدة المعرفة (﴿في لوح محفوظ﴾)")
برنامج_قاعدة = """حقائق ≔ ⟨
    "الشمس تشرق من الشرق",
    "الماء يغلي عند مئة",
    "الأرض تدور حول الشمس",
    "جمع كتاب كتب",
    "عاصمة فرنسا باريس"
⟩
⎕ "عدد الحقائق: " ⊕ نص(أحص(حقائق))
"""

ر = حلل_رموز(برنامج_قاعدة)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('ai1.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'ai1.asm', '-o', 'ai1.o'], check=True)
subprocess.run(['ld', 'ai1.o', '-o', 'ai1'], check=True)
result = subprocess.run(['./ai1'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج: {out}")

if "عدد الحقائق: 5" in out:
    print("  ✅ قاعدة المعرفة تعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 2: محرك البحث (تدبر)
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 2: محرك البحث (﴿أفلا يتدبرون﴾)")
برنامج_تدبر = """حقائق ≔ ⟨
    "الشمس تشرق من الشرق",
    "الماء يغلي عند مئة",
    "الأرض تدور حول الشمس"
⟩
سؤال ≔ "الماء"
إجابة ≔ ""
وجد ≔ 0
∀ س ∈ حقائق : ﴿ ح1 ≔ رمز(س، 0) ⋄ ح2 ≔ رمز(س، 2) ⋄ ح4 ≔ رمز(س، 4) ⋄ م ≔ (ح1 = 216) · (ح2 = 217) · (ح4 = 217) ⋄ إجابة ≔ م ؟ س : إجابة ⋄ وجد ≔ م ؟ 1 : وجد ﴾
⎕ "السؤال: " ⊕ سؤال
⎕ "الإجابة: " ⊕ إجابة
⎕ "وجد: " ⊕ نص(وجد)
"""

ر = حلل_رموز(برنامج_تدبر)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('ai2.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'ai2.asm', '-o', 'ai2.o'], check=True)
subprocess.run(['ld', 'ai2.o', '-o', 'ai2'], check=True)
result = subprocess.run(['./ai2'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "السؤال: الماء" in out and "الإجابة: الماء يغلي عند مئة" in out and "وجد: 1" in out:
    print("  ✅ محرك البحث (تدبر) يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 3: قاعدة قواعد نحوية
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 3: قواعد النحو (﴿بلسان عربي مبين﴾)")
برنامج_نحو = """قواعد ≔ ⟨
    "الفاعل مرفوع",
    "المفعول به منصوب",
    "المبتدأ مرفوع",
    "الخبر مرفوع",
    "الحال منصوب"
⟩
⎕ "عدد قواعد النحو: " ⊕ نص(أحص(قواعد))
مرفوع ≔ 0
منصوب ≔ 0
∀ س ∈ قواعد : ﴿ طول ≔ حجم(س) - 1 ⋄ ح ≔ رمز(س، طول) ⋄ م ≔ ح = 185 ⋄ مرفوع ≔ م ؟ مرفوع + 1 : مرفوع ⋄ م ≔ ح = 168 ⋄ منصوب ≔ م ؟ منصوب + 1 : منصوب ﴾
⎕ "قواعد المرفوع: " ⊕ نص(مرفوع)
⎕ "قواعد المنصوب: " ⊕ نص(منصوب)
"""

ر = حلل_رموز(برنامج_نحو)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('ai3.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'ai3.asm', '-o', 'ai3.o'], check=True)
subprocess.run(['ld', 'ai3.o', '-o', 'ai3'], check=True)
result = subprocess.run(['./ai3'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "عدد قواعد النحو: 5" in out and "قواعد المرفوع: 3" in out and "قواعد المنصوب: 2" in out:
    print("  ✅ قواعد النحو تعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 4: قاعدة رياضية (أمثال)
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 4: قاعدة رياضية (﴿يضرب مثلاً﴾)")
برنامج_رياضيات = """قواعد ≔ ⟨
    "زوجي = يقبل القسمة على 2",
    "فردي = لا يقبل القسمة على 2",
    "أولي = يقبل القسمة على 1 ونفسه فقط",
    "مربع = عدد × نفسه"
⟩
عدد ≔ 7
زوجي ≔ 0
نصف ≔ عدد ÷ 2
م ≔ نصف · 2 = عدد
زوجي ≔ م ؟ 1 : 0
⎕ "العدد: " ⊕ نص(عدد)
⎕ "زوجي؟ " ⊕ نص(زوجي)
"""

ر = حلل_رموز(برنامج_رياضيات)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('ai4.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'ai4.asm', '-o', 'ai4.o'], check=True)
subprocess.run(['ld', 'ai4.o', '-o', 'ai4'], check=True)
result = subprocess.run(['./ai4'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "العدد: 7" in out and "زوجي؟ 0" in out:
    print("  ✅ القاعدة الرياضية تعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 5: المتحقق (يقين)
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 5: المتحقق (﴿حتى يأتيك اليقين﴾)")
برنامج_يقين = """حقائق ≔ ⟨
    "2+2=4",
    "3+3=6",
    "5+5=10"
⟩
إدعاء ≔ "3+3=6"
وجد ≔ 0
∀ س ∈ حقائق : ﴿ م ≔ رمز(س، 0) = 51 ⋄ وجد ≔ م ؟ 1 : وجد ﴾
⎕ "الإدعاء: " ⊕ إدعاء
⎕ "يقين؟ " ⊕ نص(وجد)
"""

ر = حلل_رموز(برنامج_يقين)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('ai5.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'ai5.asm', '-o', 'ai5.o'], check=True)
subprocess.run(['ld', 'ai5.o', '-o', 'ai5'], check=True)
result = subprocess.run(['./ai5'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "الإدعاء: 3+3=6" in out and "يقين؟ 1" in out:
    print("  ✅ المتحقق (يقين) يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 6: حفظ قاعدة المعرفة (﴿وكل شيء أحصيناه في إمام مبين﴾)
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 6: حفظ قاعدة المعرفة (﴿في إمام مبين﴾)")
برنامج_حفظ = """حقائق ≔ ⟨
    "الشمس تشرق من الشرق",
    "الماء يغلي عند مئة",
    "الأرض تدور حول الشمس"
⟩
ملف ≔ فتح("knowledge_base.txt")
∀ س ∈ حقائق : ﴿ اكتب_ملف(ملف، س) ⋄ اكتب_ملف(ملف، " ") ﴾
اختم(ملف)
ملف ≔ فتح("knowledge_base.txt")
محتوى ≔ اقرأ_ملف(ملف، 4096)
اختم(ملف)
⎕ محتوى
"""

ر = حلل_رموز(برنامج_حفظ)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('ai6.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'ai6.asm', '-o', 'ai6.o'], check=True)
subprocess.run(['ld', 'ai6.o', '-o', 'ai6'], check=True)
result = subprocess.run(['./ai6'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج: {out}")

if "الشمس تشرق من الشرق" in out and "الماء يغلي عند مئة" in out:
    print("  ✅ حفظ قاعدة المعرفة يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ═══════════════════════════════════════════════════════════
# الملخص
# ═══════════════════════════════════════════════════════════
print("\n" + "=" * 70)
print("📖 الدستور القرآني المطبق:")
print("   ﴿وعلم آدم الأسماء كلها﴾ — قاعدة المعرفة")
print("   ﴿أفلا يتدبرون﴾ — محرك البحث (تدبر)")
print("   ﴿بلسان عربي مبين﴾ — قواعد النحو")
print("   ﴿يضرب مثلاً﴾ — القواعد الرياضية")
print("   ﴿حتى يأتيك اليقين﴾ — المتحقق (يقين)")
print("   ﴿في إمام مبين﴾ — حفظ قاعدة المعرفة")

print("\n" + "=" * 70)
if failed == 0:
    print(f"🎉 المرحلة 30 نجحت!")
    print(f"   لغتنا العربية تدعم AI الرمزي:")
    print(f"   - قاعدة معرفة (لوح محفوظ)")
    print(f"   - محرك بحث (تدبر)")
    print(f"   - قواعد نحوية (بلسان عربي مبين)")
    print(f"   - قواعد رياضية (أمثال)")
    print(f"   - متحقق (يقين)")
    print(f"   - حفظ دائم (إمام مبين)")
    print(f"\n🏆 اكتمل المشروع الكامل!")
else:
    print(f"❌ فشل {failed} اختبار")
    sys.exit(1)