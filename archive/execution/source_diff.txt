--- /home/ubuntu/work/program.cpp	2026-08-08 21:17:04.796569028 +0000
+++ /home/ubuntu/work/program_new.cpp	2026-08-08 21:45:11.821041275 +0000
@@ -273,7 +273,7 @@
         } else if (op == "&" && peek() == '&') {
             op += advance();
             tokens.push_back({TokenType::OP_AND, op, current_line, start_column});
-        } else if (op == "|" && peek() == "|") {
+        } else if (op == "|" && peek() == '|') { // تم تصحيح المقارنة لتكون مع حرف وليس سلسلة نصية
             op += advance();
             tokens.push_back({TokenType::OP_OR, op, current_line, start_column});
         } else if (op == ")") {
@@ -557,9 +557,9 @@
 }
 std::cout << "\n";
 
-// طباعة الأبناء بشكل متكرر
+// طباعة الأبناء بشكل متكرر (تم تصحيح الفاصلة العربية إلى إنجليزية)
 for (const auto& ابن : العقدة->الأبناء) {
-    طباعة_الشجرة(ابن، المسافة_البادئة + 1);
+    طباعة_الشجرة(ابن, المسافة_البادئة + 1);
 }
 }
 
@@ -1304,7 +1304,8 @@
         نوع_العقدة::معرفات_استعلام, "معرفات",
         الإعراب::منصوب, الموقع_النحوي::مفعول_به
     );
-    // قراءة المعرفات المفصولة بفواصل
+    
+    // قراءة المعرفات المفصولة بفواصل (تم تصحيح المنطق ليفصل بين التحقق والاستهلاك)
     do {
         Token معرف = استهلك(TokenType::IDENTIFIER, "خطأ: يُتوقع اسم معرف بعد 'استخرج'");
         المعرفات->أضف_ابناً(new العقدة_النحوية(
@@ -1312,7 +1313,10 @@
             الإعراب::منصوب, الموقع_النحوي::مفعول_به,
             معرف.line, معرف.column
         ));
-    } while (يطابق(TokenType::COMMA) && تقدم());
+        if (!يطابق(TokenType::COMMA)) break;
+        تقدم(); // استهلاك الفاصلة
+    } while (true);
+    
     استعلام->أضف_ابناً(المعرفات);
 
     // من المصدر (المصدر مجرور)
@@ -1491,7 +1495,7 @@
         الإعراب::غير_معرب, الموقع_النحوي::غير_محدد
     );
     if (!يطابق(TokenType::PAREN_CLOSE)) {
-        // قراءة كل معامل: الاسم: النوع
+        // قراءة كل معامل: الاسم: النوع (تم تصحيح المنطق ليفصل بين التحقق والاستهلاك)
         do {
             Token اسم_معامل = استهلك(TokenType::IDENTIFIER, "خطأ: يُتوقع اسم المعامل");
             العقدة_النحوية* معامل = new العقدة_النحوية(
@@ -1509,7 +1513,9 @@
                 ));
             }
             معاملات->أضف_ابناً(معامل);
-        } while (يطابق(TokenType::COMMA) && تقدم());
+            if (!يطابق(TokenType::COMMA)) break;
+            تقدم(); // استهلاك الفاصلة
+        } while (true);
     }
     استهلك(TokenType::PAREN_CLOSE, "خطأ: يُتوقع '(' لإغلاق معاملات الدالة");
     عقدة_الدالة->أضف_ابناً(معاملات);
