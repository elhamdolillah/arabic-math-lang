#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
المرحلة 38: توليد نصوص (بيان)
الدستور: ﴿علمه البيان﴾
"""
import sys, subprocess
sys.path.insert(0, '.')
from math_complete import *

# ═══════════════════════════════════════════════════════════
print("=" * 70)
print("المرحلة 38: توليد نصوص (بيان)")
print("الدستور: ﴿علمه البيان﴾")
print("=" * 70)

failed = 0

# ─────────────────────────────────────────────────────────
# اختبار 1: توليد جملة اسمية من قالب
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 1: توليد جملة اسمية (﴿علمه البيان﴾)")
برنامج_اسمية = """اسم ≔ "طالب"
خبر ≔ "مجتهد"
جملة ≔ "ال" ⊕ اسم ⊕ " " ⊕ خبر
⎕ جملة
"""

ر = حلل_رموز(برنامج_اسمية)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('gen1.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'gen1.asm', '-o', 'gen1.o'], check=True)
subprocess.run(['ld', 'gen1.o', '-o', 'gen1'], check=True)
result = subprocess.run(['./gen1'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج: {out}")

if out == "الطالب مجتهد":
    print("  ✅ توليد الجملة الاسمية يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 2: توليد جملة فعلية
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 2: توليد جملة فعلية (﴿وقل رب زدني علماً﴾)")
برنامج_فعلية = """فعل ≔ "يكتب"
فاعل ≔ "الطالب"
مفعول ≔ "الدرس"
جملة ≔ فعل ⊕ " " ⊕ فاعل ⊕ " " ⊕ مفعول
⎕ جملة
"""

ر = حلل_رموز(برنامج_فعلية)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('gen2.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'gen2.asm', '-o', 'gen2.o'], check=True)
subprocess.run(['ld', 'gen2.o', '-o', 'gen2'], check=True)
result = subprocess.run(['./gen2'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج: {out}")

if out == "يكتب الطالب الدرس":
    print("  ✅ توليد الجملة الفعلية يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 3: توليد جمل متعددة من نفس القالب
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 3: توليد متعدد (﴿ألم نجعل له عينين ولساناً وشفتين﴾)")
برنامج_متعدد = """أسماء ≔ ⟨"أحمد", "فاطمة", "علي"⟩
خبر ≔ " مجتهد"
جملة١ ≔ رأس(أسماء) ⊕ خبر
جملة٢ ≔ رأس(ذيل(أسماء)) ⊕ خبر
جملة٣ ≔ رأس(ذيل(ذيل(أسماء))) ⊕ خبر
⎕ جملة١
⎕ جملة٢
⎕ جملة٣
"""

ر = حلل_رموز(برنامج_متعدد)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('gen3.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'gen3.asm', '-o', 'gen3.o'], check=True)
subprocess.run(['ld', 'gen3.o', '-o', 'gen3'], check=True)
result = subprocess.run(['./gen3'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "أحمد مجتهد" in out and "فاطمة مجتهد" in out and "علي مجتهد" in out:
    print("  ✅ التوليد المتعدد يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 4: توليد فقرة من جمل
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 4: توليد فقرة (﴿وإن تعدوا نعمة الله لا تحصوها﴾)")
برنامج_فقرة = """جملة١ ≔ "العلم نور"
جملة٢ ≔ "الجهل ظلام"
جملة٣ ≔ "اطلبوا العلم من المهد إلى اللحد"
فقرة ≔ جملة١ ⊕ ". " ⊕ جملة٢ ⊕ ". " ⊕ جملة٣ ⊕ "."
⎕ فقرة
"""

ر = حلل_رموز(برنامج_فقرة)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('gen4.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'gen4.asm', '-o', 'gen4.o'], check=True)
subprocess.run(['ld', 'gen4.o', '-o', 'gen4'], check=True)
result = subprocess.run(['./gen4'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج: {out}")

if "العلم نور. الجهل ظلام. اطلبوا العلم من المهد إلى اللحد." in out:
    print("  ✅ توليد الفقرة يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 5: توليد نص من قوائم (اختيار مكونات)
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 5: توليد من قوائم (﴿فاختار موسى سبعين رجلاً﴾)")
برنامج_قوائم = """مبتدآت ≔ ⟨"العلم", "العمل", "الصبر"⟩
أخبار ≔ ⟨"نور", "عبادة", "مفتاح"⟩
مبتدأ ≔ رأس(مبتدآت)
خبر ≔ رأس(أخبار)
جملة ≔ مبتدأ ⊕ " " ⊕ خبر
⎕ جملة
مبتدأ ≔ رأس(ذيل(مبتدآت))
خبر ≔ رأس(ذيل(أخبار))
جملة ≔ مبتدأ ⊕ " " ⊕ خبر
⎕ جملة
مبتدأ ≔ رأس(ذيل(ذيل(مبتدآت)))
خبر ≔ رأس(ذيل(ذيل(أخبار)))
جملة ≔ مبتدأ ⊕ " " ⊕ خبر
⎕ جملة
"""

ر = حلل_رموز(برنامج_قوائم)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('gen5.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'gen5.asm', '-o', 'gen5.o'], check=True)
subprocess.run(['ld', 'gen5.o', '-o', 'gen5'], check=True)
result = subprocess.run(['./gen5'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "العلم نور" in out and "العمل عبادة" in out and "الصبر مفتاح" in out:
    print("  ✅ التوليد من قوائم يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 6: حفظ النصوص المولدة
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 6: حفظ النصوص المولدة (﴿في إمام مبين﴾)")
برنامج_حفظ = """اسم ≔ "طالب"
خبر ≔ "مجتهد"
جملة ≔ "ال" ⊕ اسم ⊕ " " ⊕ خبر
ملف ≔ فتح("generated_text.txt")
اكتب_ملف(ملف، جملة)
اكتب_ملف(ملف، " ")
اكتب_ملف(ملف، "العلم نور")
اكتب_ملف(ملف، " ")
اكتب_ملف(ملف، "العمل عبادة")
اختم(ملف)
ملف ≔ فتح("generated_text.txt")
محتوى ≔ اقرأ_ملف(ملف، 4096)
اختم(ملف)
⎕ محتوى
"""

ر = حلل_رموز(برنامج_حفظ)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('gen6.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'gen6.asm', '-o', 'gen6.o'], check=True)
subprocess.run(['ld', 'gen6.o', '-o', 'gen6'], check=True)
result = subprocess.run(['./gen6'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج: {out}")

if "الطالب مجتهد" in out and "العلم نور" in out and "العمل عبادة" in out:
    print("  ✅ حفظ النصوص المولدة يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ═══════════════════════════════════════════════════════════
# الملخص
# ═══════════════════════════════════════════════════════════
print("\n" + "=" * 70)
print("📖 الدستور القرآني المطبق:")
print("   ﴿علمه البيان﴾ — توليد الجملة الاسمية")
print("   ﴿وقل رب زدني علماً﴾ — توليد الجملة الفعلية")
print("   ﴿ألم نجعل له عينين ولساناً وشفتين﴾ — التوليد المتعدد")
print("   ﴿وإن تعدوا نعمة الله لا تحصوها﴾ — توليد الفقرة")
print("   ﴿فاختار موسى سبعين رجلاً﴾ — التوليد من قوائم")
print("   ﴿في إمام مبين﴾ — حفظ النصوص المولدة")

print("\n" + "=" * 70)
if failed == 0:
    print(f"🎉 المرحلة 38 نجحت!")
    print(f"   لغتنا العربية تدعم توليد نصوص كامل:")
    print(f"   - توليد جملة اسمية من قالب")
    print(f"   - توليد جملة فعلية")
    print(f"   - توليد جمل متعددة من نفس القالب")
    print(f"   - توليد فقرة من جمل")
    print(f"   - توليد نص من قوائم")
    print(f"   - حفظ النصوص المولدة في ملف")
    print(f"\n🏆 اكتمل المستوى 7: AI المتقدم!")
    print(f"   الخطوة التالية: المستوى 8 — التوثيق والنشر")
else:
    print(f"❌ فشل {failed} اختبار")
    sys.exit(1)