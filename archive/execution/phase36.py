#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
المرحلة 36: التعلم الرمزي (اعتبار)
الدستور: ﴿فاعتبروا يا أولي الأبصار﴾
"""
import sys, subprocess
sys.path.insert(0, '.')
from math_complete import *

# ═══════════════════════════════════════════════════════════
print("=" * 70)
print("المرحلة 36: التعلم الرمزي (اعتبار)")
print("الدستور: ﴿فاعتبروا يا أولي الأبصار﴾")
print("=" * 70)

failed = 0

# ─────────────────────────────────────────────────────────
# اختبار 1: استقراء قاعدة الجمع
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 1: استقراء قاعدة الجمع (﴿إن في خلق السماوات لآيات﴾)")
برنامج_جمع = """أمثلة_أ ≔ ⟨2, 4, 3⟩
أمثلة_ب ≔ ⟨3, 1, 3⟩
أمثلة_نتيجة ≔ ⟨5, 5, 6⟩
صحيحة ≔ 0
مجموع_أ ≔ رأس(أمثلة_أ)
مجموع_ب ≔ رأس(أمثلة_ب)
مجموع_نتيجة ≔ رأس(أمثلة_نتيجة)
م ≔ (مجموع_أ + مجموع_ب) = مجموع_نتيجة
صحيحة ≔ م ؟ صحيحة + 1 : صحيحة
مجموع_أ ≔ رأس(ذيل(أمثلة_أ))
مجموع_ب ≔ رأس(ذيل(أمثلة_ب))
مجموع_نتيجة ≔ رأس(ذيل(أمثلة_نتيجة))
م ≔ (مجموع_أ + مجموع_ب) = مجموع_نتيجة
صحيحة ≔ م ؟ صحيحة + 1 : صحيحة
مجموع_أ ≔ رأس(ذيل(ذيل(أمثلة_أ)))
مجموع_ب ≔ رأس(ذيل(ذيل(أمثلة_ب)))
مجموع_نتيجة ≔ رأس(ذيل(ذيل(أمثلة_نتيجة)))
م ≔ (مجموع_أ + مجموع_ب) = مجموع_نتيجة
صحيحة ≔ م ؟ صحيحة + 1 : صحيحة
⎕ "الأمثلة الصحيحة: " ⊕ نص(صحيحة) ⊕ " من 3"
⎕ "القاعدة المستنتجة: جمع(أ، ب) = أ + ب"
"""

ر = حلل_رموز(برنامج_جمع)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('learn1.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'learn1.asm', '-o', 'learn1.o'], check=True)
subprocess.run(['ld', 'learn1.o', '-o', 'learn1'], check=True)
result = subprocess.run(['./learn1'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "الأمثلة الصحيحة: 3 من 3" in out:
    print("  ✅ استقراء قاعدة الجمع يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 2: استقراء قاعدة الزوجية
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 2: استقراء قاعدة الزوجية (﴿وأنه خلق الزوجين﴾)")
برنامج_زوجية = """أعداد ≔ ⟨2, 4, 3, 6, 5⟩
زوجي ≔ 0
فردي ≔ 0
س ≔ رأس(أعداد)
م ≔ (س ÷ 2) · 2 = س
زوجي ≔ م ؟ زوجي + 1 : زوجي
فردي ≔ م ؟ فردي : فردي + 1
س ≔ رأس(ذيل(أعداد))
م ≔ (س ÷ 2) · 2 = س
زوجي ≔ م ؟ زوجي + 1 : زوجي
فردي ≔ م ؟ فردي : فردي + 1
س ≔ رأس(ذيل(ذيل(أعداد)))
م ≔ (س ÷ 2) · 2 = س
زوجي ≔ م ؟ زوجي + 1 : زوجي
فردي ≔ م ؟ فردي : فردي + 1
س ≔ رأس(ذيل(ذيل(ذيل(أعداد))))
م ≔ (س ÷ 2) · 2 = س
زوجي ≔ م ؟ زوجي + 1 : زوجي
فردي ≔ م ؟ فردي : فردي + 1
س ≔ رأس(ذيل(ذيل(ذيل(ذيل(أعداد)))))
م ≔ (س ÷ 2) · 2 = س
زوجي ≔ م ؟ زوجي + 1 : زوجي
فردي ≔ م ؟ فردي : فردي + 1
⎕ "الأعداد: ⟨2, 4, 3, 6, 5⟩"
⎕ "زوجي: " ⊕ نص(زوجي)
⎕ "فردي: " ⊕ نص(فردي)
⎕ "القاعدة: زوجي(س) ⟺ (س÷2)·2 = س"
"""

ر = حلل_رموز(برنامج_زوجية)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('learn2.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'learn2.asm', '-o', 'learn2.o'], check=True)
subprocess.run(['ld', 'learn2.o', '-o', 'learn2'], check=True)
result = subprocess.run(['./learn2'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "زوجي: 3" in out and "فردي: 2" in out:
    print("  ✅ استقراء قاعدة الزوجية يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 3: تعميم القاعدة على أمثلة جديدة
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 3: تعميم القاعدة (﴿فاعتبروا يا أولي الأبصار﴾)")
برنامج_تعميم = """قاعدة_أ ≔ 10
قاعدة_ب ≔ 20
نتيجة_متوقعة ≔ 30
نتيجة_فعلية ≔ قاعدة_أ + قاعدة_ب
م ≔ نتيجة_فعلية = نتيجة_متوقعة
⎕ "تعميم 1: جمع(10، 20) = " ⊕ نص(نتيجة_فعلية) ⊕ (م ؟ " ✓" : " ✗")
قاعدة_أ ≔ 7
قاعدة_ب ≔ 8
نتيجة_متوقعة ≔ 15
نتيجة_فعلية ≔ قاعدة_أ + قاعدة_ب
م ≔ نتيجة_فعلية = نتيجة_متوقعة
⎕ "تعميم 2: جمع(7، 8) = " ⊕ نص(نتيجة_فعلية) ⊕ (م ؟ " ✓" : " ✗")
س ≔ 8
نتيجة_متوقعة ≔ 1
م ≔ (س ÷ 2) · 2 = س
زوجي ≔ م ؟ 1 : 0
⎕ "تعميم 3: زوجي(8) = " ⊕ نص(زوجي) ⊕ (زوجي = نتيجة_متوقعة ؟ " ✓" : " ✗")
"""

ر = حلل_رموز(برنامج_تعميم)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('learn3.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'learn3.asm', '-o', 'learn3.o'], check=True)
subprocess.run(['ld', 'learn3.o', '-o', 'learn3'], check=True)
result = subprocess.run(['./learn3'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "تعميم 1: جمع(10، 20) = 30 ✓" in out and "تعميم 2: جمع(7، 8) = 15 ✓" in out and "تعميم 3: زوجي(8) = 1 ✓" in out:
    print("  ✅ تعميم القاعدة يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 4: تقييم دقة القاعدة
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 4: تقييم الدقة (﴿وكل شيء عنده بمقدار﴾)")
برنامج_دقة = """صحيحة ≔ 3
خاطئة ≔ 0
إجمالي ≔ صحيحة + خاطئة
دقة ≔ صحيحة · 100 ÷ إجمالي
⎕ "أمثلة صحيحة: " ⊕ نص(صحيحة)
⎕ "أمثلة خاطئة: " ⊕ نص(خاطئة)
⎕ "الدقة: " ⊕ نص(دقة) ⊕ "%"
"""

ر = حلل_رموز(برنامج_دقة)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('learn4.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'learn4.asm', '-o', 'learn4.o'], check=True)
subprocess.run(['ld', 'learn4.o', '-o', 'learn4'], check=True)
result = subprocess.run(['./learn4'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "الدقة: 100%" in out:
    print("  ✅ تقييم الدقة يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 5: تعلم من الخطأ (تصحيح القاعدة)
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 5: تعلم من الخطأ (﴿إن الله لا يضيع أجر المحسنين﴾)")
برنامج_خطأ = """قاعدة ≔ 2
مثال ≔ 5
نتيجة_متوقعة ≔ 10
نتيجة_فعلية ≔ قاعدة · مثال
خطأ ≔ نتيجة_متوقعة - نتيجة_فعلية
⎕ "القاعدة القديمة: ضرب في 2"
⎕ "المثال: 5، المتوقع: 10، الفعلي: " ⊕ نص(نتيجة_فعلية)
⎕ "الخطأ: " ⊕ نص(خطأ)
م ≔ خطأ = 0
قاعدة_جديدة ≔ م ؟ قاعدة : نتيجة_متوقعة ÷ مثال
⎕ "القاعدة الجديدة: ضرب في " ⊕ نص(قاعدة_جديدة)
"""

ر = حلل_رموز(برنامج_خطأ)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('learn5.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'learn5.asm', '-o', 'learn5.o'], check=True)
subprocess.run(['ld', 'learn5.o', '-o', 'learn5'], check=True)
result = subprocess.run(['./learn5'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج:\n{out}")

if "القاعدة الجديدة: ضرب في 2" in out:
    print("  ✅ تعلم من الخطأ يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ─────────────────────────────────────────────────────────
# اختبار 6: حفظ القواعد المستنتجة
# ─────────────────────────────────────────────────────────
print("\n🧪 اختبار 6: حفظ القواعد (﴿في إمام مبين﴾)")
برنامج_حفظ = """ملف ≔ فتح("learned_rules.txt")
اكتب_ملف(ملف، "قاعدة 1: جمع(أ،ب)=أ+ب دقة=100%")
اكتب_ملف(ملف، " قاعدة 2: زوجي(س)=(س÷2)·2=س")
اكتب_ملف(ملف، " قاعدة 3: ضرب(أ،ب)=أ·ب")
اختم(ملف)
ملف ≔ فتح("learned_rules.txt")
محتوى ≔ اقرأ_ملف(ملف، 4096)
اختم(ملف)
⎕ محتوى
"""

ر = حلل_رموز(برنامج_حفظ)
ب = حلل_برنامج(ر)
asm = compile_program(ب)
with open('learn6.asm', 'w') as f:
    f.write(asm)
subprocess.run(['nasm', '-f', 'elf64', 'learn6.asm', '-o', 'learn6.o'], check=True)
subprocess.run(['ld', 'learn6.o', '-o', 'learn6'], check=True)
result = subprocess.run(['./learn6'], capture_output=True, text=True)
out = result.stdout.strip()
print(f"  الخرج: {out}")

if "قاعدة 1: جمع(أ،ب)=أ+ب دقة=100%" in out and "قاعدة 3: ضرب(أ،ب)=أ·ب" in out:
    print("  ✅ حفظ القواعد يعمل!")
else:
    print("  ❌ فشل")
    failed += 1

# ═══════════════════════════════════════════════════════════
# الملخص
# ═══════════════════════════════════════════════════════════
print("\n" + "=" * 70)
print("📖 الدستور القرآني المطبق:")
print("   ﴿فاعتبروا يا أولي الأبصار﴾ — الاستقراء")
print("   ﴿إن في خلق السماوات لآيات﴾ — اكتشاف الأنماط")
print("   ﴿وأنه خلق الزوجين﴾ — قاعدة الزوجية")
print("   ﴿وكل شيء عنده بمقدار﴾ — تقييم الدقة")
print("   ﴿إن الله لا يضيع أجر المحسنين﴾ — تعلم من الخطأ")
print("   ﴿في إمام مبين﴾ — حفظ القواعد")

print("\n" + "=" * 70)
if failed == 0:
    print(f"🎉 المرحلة 36 نجحت!")
    print(f"   لغتنا العربية تدعم التعلم الرمزي:")
    print(f"   - استقراء قاعدة الجمع من أمثلة")
    print(f"   - استقراء قاعدة الزوجية")
    print(f"   - تعميم القاعدة على أمثلة جديدة")
    print(f"   - تقييم دقة القاعدة")
    print(f"   - تعلم من الخطأ (تصحيح القاعدة)")
    print(f"   - حفظ القواعد المستنتجة")
    print(f"\n   الخطوة التالية: المرحلة 37 — NLP عربي كامل (لسان)")
else:
    print(f"❌ فشل {failed} اختبار")
    sys.exit(1)