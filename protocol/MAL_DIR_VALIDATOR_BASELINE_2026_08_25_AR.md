# نقطة التجميد الرسمية لهجرة مدقق MAL-DIR

**اسم النقطة:** `uori-mal-dir-validator-freeze-2026-08-25`

**النطاق:** امتداد `extensions/mal_dir_validator.ar`، عقد الساحة المسطحة، المقارنة الثنائية، وأدلة التكامل المستمر الخاصة بالامتداد.

**المرجع الأبوي:** `uori-wave77-freeze-2026-08-24`، وكان مرجعه السابق `f56b8bd`.

**حالة النواة العامة:** لم تُعدّل Grammar v0.1 ولا baseline النواة العامة. هذه نقطة تجميد مستقلة ومحدودة بالامتداد، ولا تعني اعتماد الميزات البحثية الأخرى.

**دليل القبول:**

| الحاجز | النتيجة |
|---|---|
| تجميع MAL | `PASS` |
| تنفيذ fixture العربي | `PASS` |
| المقارنة الثنائية | `CASES=8`, `MATCHES=8`, `FAILURES=0` |
| الاختبارات الرسمية المرتبطة | `PASS` |
| pytest | غير متاح في البيئة: `No module named pytest`؛ استُخدم مسار unittest، ولم توجد اختبارات unittest قابلة للاكتشاف |
| قياس نسبة اللغة | `PASS` |
| فحص workflow | `PASS` |
| الترفيع التلقائي | `DENY` |

**التصنيف:** `EXTENSION_SCOPED_PROVEN` لمسار fixture المحدد فقط، مع بقاء التحقق العام للعقد الداخلية وتصنيف النواة العامة خارج هذا الإثبات.

**سياسة الدمج:** لا يُرفع هذا التجميد إلى baseline النواة العامة ولا إلى wheel أو WASM دون مراجعة مستقلة لاحقة.
